{
  "name": "atalaya",
  "private": true,
  "version": "0.0.1",
  "description": "Server management and monitoring panel",
  "scripts": {
    "back": "npm --prefix backend run start:dev",
    "front": "npm --prefix frontend start",
    "dev": "concurrently -n back,front -c blue,magenta \"npm --prefix backend run start:dev\" \"npm --prefix frontend start\"",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "prisma:generate": "npm --prefix backend run prisma:generate",
    "prisma:migrate": "npm --prefix backend run prisma:migrate"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
